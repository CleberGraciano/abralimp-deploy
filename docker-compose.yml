version: '3.8'

services:
  frontend:
    build:
      context: .
    image: versao-bralimp-frontend:latest
    ports:
      - "88:80"
    restart: unless-stopped
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
